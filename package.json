{
  "name": "court2go",
  "version": "0.1.0",
  "private": true,
  "description": "court2go — multi-tenant court/venue booking SaaS (Turborepo monorepo). See docs/ARCHITECTURE.md.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
