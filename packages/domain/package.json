{
  "name": "@repo/domain",
  "version": "0.1.0",
  "private": true,
  "description": "court2go pure domain logic — grid/pricing/hold business rules shared by apps/api (M4 availability, M6 booking/hold). No I/O, no NestJS; deterministic functions over @repo/types shapes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@repo/types": "*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "typescript": "^5.5.4"
  }
}
