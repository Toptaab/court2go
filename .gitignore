# secrets — never commit
.env
.env.*
!.env.example

# os / editor
.DS_Store

# deps / build (monorepo — populated later)
node_modules/
dist/
.next/
.turbo/
coverage/

# generated Prisma Client (prisma-client generator, v7+) — regenerated by
# `prisma generate`, never hand-edited or committed.
apps/api/src/generated/
